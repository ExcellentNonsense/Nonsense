@model IEnumerable<User>

@{
  ViewBag.Title = "Пользователи";
}

<div class="p-1 bg-primary text-white"><h4>Пользователи</h4></div>
<div class="text-danger" asp-validation-summary="ModelOnly"></div>
<table class="table table-sm table-bordered">
  <tr><th>ID</th><th>Имя</th><th>Email</th></tr>
  @if (Model.Count() == 0) {
    <tr><td colspan="3" class="text-center">Пользователей нет</td></tr>
  }
  else {
    foreach (User user in Model) {
      <tr>
        <td>@user.Id</td>
        <td>@user.UserName</td>
        <td>@user.Email</td>
        <td>
          <a asp-action="Display" asp-route-id="@user.Id" class="btn btn-sm btn-info">Карточка</a>
          <a asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-info">Изменить</a>
          <form asp-action="Delete" asp-route-id="@user.Id" method="post" class="d-inline-block">
            <button type="submit" class="btn btn-sm btn-danger">Удалить</button>
          </form>
        </td>
      </tr>
    }
  }
</table>
<a class="btn btn-primary" asp-action="Create">Создать</a>